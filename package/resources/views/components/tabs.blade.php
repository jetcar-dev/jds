@props(['name'=>null,'value'=>null,'variant'=>'solid','color'=>'default','size'=>'md','radius'=>'md','orientation'=>'horizontal','keyboardActivation'=>'automatic','fullWidth'=>false])
<div data-slot="tabs" data-variant="{{ $variant }}" data-color="{{ $color }}" data-orientation="{{ $orientation }}" data-keyboard-activation="{{ $keyboardActivation }}" style="{{ $fullWidth?'width:100%;':'' }}" {{ $attributes->class("app-tabs app-color-$color app-size-$size app-radius-$radius") }}>@if($name)<input type="hidden" data-tabs-input name="{{ $name }}" value="{{ $value }}">@endif{{ $slot }}</div>

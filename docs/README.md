# JDS Docs

JetCar Design System의 Blade 컴포넌트를 확인하는 Laravel 문서 앱입니다.
데이터베이스 없이 실행되며 `../package`의 원본 CSS와 JavaScript를 직접 불러옵니다.

```bash
composer install
npm install
composer run jds-assets
composer run dev
```

테스트는 `composer test`로 실행합니다.
